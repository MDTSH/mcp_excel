PyXLL **5.12.4** 64-bit add-ins, one folder per CPython ABI (`py39` … `py313`).

`install_mcp_excel.py` copies the matching `py3xx\pyxll.xll` over `..\pyxll.xll` after you pick a Python. Excel always loads `lib\X64\pyxll.xll`.
